# Project-specific model overrides
modelRoles:
  default: ollama-cloud/glm-5.2:high
  slow: ollama-cloud/kimi-k2.7-code:high

# Project-specific tool approval
tools:
  approvalMode: write
  approval:
    bash: prompt

# Project-specific disabled providers (replaces global array)
disabledProviders:
  - rule:rs-box-leak
  - rule:rs-future-prelude
  - rule:rs-lazylock
  - rule:rs-match-ergonomics
  - rule:rs-parking-lot
  - rule:rs-result-type