# Workflow

When you're working with the library (lib/), you always have to update the related docs.
When you made some changes to the library, you always have to add tests to the tests/ folder.

When you made some changes, you always have to run `bun run format` and `bun run lint`.
You must not run dev or build scripts `bun run build` or `bun run dev`.
