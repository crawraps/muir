import type { SurfaceConfig } from '..'
import themeDefaults from './theme-defaults.json'

export const defaultSurfaceLight: SurfaceConfig = themeDefaults.surfaceLight as SurfaceConfig
export const defaultSurfaceDark: SurfaceConfig = themeDefaults.surfaceDark as SurfaceConfig
